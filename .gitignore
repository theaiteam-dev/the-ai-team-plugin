# Mission state is project-specific, not part of plugin
mission/

# Dependencies
node_modules/

# Bun
bun.lockb

# Local settings
.claude/settings.local.json

# OS files
.DS_Store

# Draft docs
docs/kanban-ui-prd.md

# Untracked nested directory (git submodule artifact)
The-Ai-team/

# Review docs (not part of plugin)
REVIEW_TEAMMATE_TOOL_INTEGRATION.md

# Downloaded CLI binary (wrapper script bin/ateam is tracked)
bin/ateam-bin

# Local SQLite databases — never commit runtime DB files (any depth)
*.db
*.db-journal
*.db-wal
*.db-shm
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Observer hook failure log (runtime telemetry, not source)
.ateam-observer-failures.log

# Tool-generated project metadata, not source (devtrack, project scaffolding)
/devtrack.yaml
/project.yaml

# Private prod deployment runbook — references personal infra (arcane-k8s,
# cluster URLs, GHCR org). Kept local; this is a public/OSS repo.
docs/PROD-DEPLOY.md
