{
  "name": "@ai-team/root",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A(i)-Team monorepo workspace root",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter './packages/*' build",
    "test": "vitest run",
    "test:all": "bun run --filter './packages/*' test",
    "test:e2e": "flowspec run"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.6.0"
  }
}
